#include <vector>
#include <iostream>
using namespace std;

// You have to keep the original order of all non-zeros elements
void MoveZeroes(vector<int>& nums) {
Le
}

int main() {
    vector<int> input = {0,1,0,3,12};
    MoveZeroes(input);
    for (auto i : input) {
        cout << i << endl;
    }
    return 0;
}
